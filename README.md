# company-structure

### Install dependencies by running on the root
`pnpm i`

### Run the project by
`pnpm dev`

### Visit this link in your browser
`http://localhost:3000`

### to run tests, please run
`pnpm test`
